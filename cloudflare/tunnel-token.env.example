# Legacy example env file (not used by default with /etc/homelab/host-secrets/cloudflared.env flow).
# Keep only for compatibility if you run cloudflared outside the NixOS module.
# Dashboard-managed tunnel token from Cloudflare Zero Trust -> Networks -> Tunnels.
CLOUDFLARE_TUNNEL_TOKEN=REPLACE_WITH_DASHBOARD_TUNNEL_TOKEN
