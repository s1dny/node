apiVersion: v1
kind: Secret
metadata:
  name: kopia-auth
  namespace: backup
type: Opaque
stringData:
  KOPIA_REPOSITORY_PASSWORD: "REPLACE"
  KOPIA_SERVER_HOSTNAME: "REPLACE"
  KOPIA_SERVER_USERNAME: "REPLACE"
  KOPIA_SERVER_PASSWORD: "REPLACE"
